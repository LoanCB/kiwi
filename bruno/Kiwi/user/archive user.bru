meta {
  name: archive user
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/users/{{id}}/archive
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "isActive": false
  }
}

vars:pre-request {
  id: 2
}
